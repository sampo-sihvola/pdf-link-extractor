## pdf link extractor

python script that reads pdf files and extracts the links into a txt file.

copy a pdf file into the folder and run the script.

## requirements

install python3, then:

``pip install pypdf``